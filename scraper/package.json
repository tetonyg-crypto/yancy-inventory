{
    "name": "yancy-inventory-scraper",
    "version": "2.0.0",
    "private": true,
    "scripts": {
        "sync": "node sync.js",
        "sync:push": "node sync.js --push",
        "admin": "node ../admin/server.js"
    },
    "dependencies": {
        "puppeteer": "^22.0.0"
    }
}
